# Registry of operator-approved company-shaped literals in cs/.
#
# tests/run.sh step 1 scans cs/ against a wordlist of company-shaped strings
# (mailbox domain, slugs, the operator's name, etc). The wordlist only PROPOSES
# candidates — it does not judge them. The operator disposes: each line below
# is a literal he has explicitly reviewed and approved, with a reason on
# record. A hit that is NOT listed here fails CI as a proposal for his review.
#
# Format, one approval per line:
#   path :: exact line content :: reason
# `#` comments and blank lines are ignored. The path and content must match a
# grep hit exactly (content is whitespace-trimmed before comparison); the line
# number is deliberately NOT part of the key, because line numbers drift.
#
# Never add an entry here without the operator's explicit decision.
#
# One class of hit is decided by PATTERN in tests/run.sh instead of line by line
# here: the platform brand where it names shared infrastructure the kernel
# drives — the mrcall-desktop engine, the mrcall-tracking adapter id, the
# mrcall.search_businesses RPC method. The charter (CLAUDE.md rule 1) already
# blesses those three forms for every clone, and they occur on ~50 lines.
# Copying 50 exact lines in here would bury the handful of genuine proposals
# among them, and every one of the 50 would go stale on the next reword. Every
# OTHER use of the brand still lands here as a proposal.

cs/templates/project/requirements.txt.j2 :: cs-kernel @ git+https://github.com/malemi/cs-kernel@v{{ repo_kernel_version }} :: operator-approved 2026-08-09 — "malemi" is the kernel's own public home; shared infrastructure, identical for every clone, not clone data
cs/templates/project/README.md.j2 :: kernel README, [The `cs` CLI](https://github.com/malemi/cs-kernel#the-cs-cli). :: same recorded decision as requirements.txt.j2 (operator-approved 2026-08-09): "malemi" is the kernel's own public home — this line links the clone README to the kernel README's CLI reference, identical for every clone
cs/config.py :: sms_proxy_base: str = "https://zylch.mrcall.ai/api/desktop/sms/send" :: operator-approved 2026-08-24 — the mrcall-desktop engine's SMS send path. Same category the charter already blesses for the engine itself and the mrcall.search_businesses RPC: shared infrastructure the kernel drives, identical for every clone, not clone data. SMS bills against the platform CALLCREDIT pool whichever clone sent it, so there is nothing per-company to configure. It is a kernel DEFAULT, not a required field: a clone points elsewhere by declaring [sms].proxy_base, and [sms].enabled remains the switch
