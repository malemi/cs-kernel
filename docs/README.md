# Transversal Documentation Index

<!-- doc-scope:start -->
Scope: the router for the transversal documents under `docs/`. It navigates and
nothing else — the repo's inventory, roles and ownership live only in the index
file (`../AGENTS.md`), and volatile state only in `active-context.md`.
<!-- doc-scope:end -->

Index of transversal docs; the repo inventory / roles / ownership live only in the index file (`AGENTS.md`).

- [release-procedure.md](release-procedure.md) — **read before any release or clone upgrade**: the ordered procedure, the inventory of every file that carries a version claim, and the mandatory multi-version grep sweep.
- [agent-skills.md](agent-skills.md) — the non-negotiable three-host contract: every skill change reaches and is verified on Claude Code, Codex, and OpenCode in the same change.
- [active-context-archive.md](active-context-archive.md) — pruned session narrative from `active-context.md`, dated, newest-first; cold storage, never read by `/doc-start`.
